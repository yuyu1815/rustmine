# AI Resume Pointer

This file is kept only for older links.

Mutable recovery state no longer lives in `docs/ai/`. Read:

```text
docs/next/README.md
```

`docs/ai/` is the fixed startup and routing layer. Put current location, next
task, blockers, and recovery pointers in `docs/next/`. Put shared evidence,
proof status, decisions, and analysis notes in the owning `docs/analysis/`
shard.
